# Claude Code worktrees (temporary agent workspaces)
.claude/

# R session files
.Rhistory
.RData
.Rproj.user/

# Test artifacts (R creates Rplots.pdf when a chunk plots without an
# explicit device; not part of the test corpus).
tests/testthat/Rplots.pdf

# R CMD check and build artifacts
*.Rcheck/
*.tar.gz

# Generated report output (standalone mode runs)
reports/runs/

# CI uploads the latest verify log as a workflow artifact; don't commit per-run logs.
audit/verify_report_*.txt

# OS files
.DS_Store
Thumbs.db
.positai
